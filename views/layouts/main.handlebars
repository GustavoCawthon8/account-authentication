<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Auth - Gustavo cawthon</title>
</head>
<body>
    <header>
        <ul>
            <li><a href="/">Home</a></li>
        {{#if session.userid}}
            <li><a href="/logout">Logout</a></li>
        {{else}}
            <li><a href="/login">Entrar</a></li>
            <li><a href="/register">Registrar</a></li>
        {{/if}}
        </ul>
    </header>

    <div class="container">
        {{{body}}}

            
        {{#if session.userid}}
           <p>Logado</p>
        {{/if}}
    </div>
</body>
</html>